load("@pip//:requirements.bzl", "requirement")
load("@rules_python//python:defs.bzl", "py_binary")

py_binary(
    name = "cc_generator",
    srcs = ["cc_generator.py"],
    main = "cc_generator.py",
    deps = [
        requirement("colored"),
    ],
)
